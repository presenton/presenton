# P10. Убрать мёртвое поле trigger_webhook

Ветка: `feat/remove-trigger-webhook`
Статус: закрыта, влита в `main`.

---

## Что сделано

- `GeneratePresentationRequest.trigger_webhook` удалён из pydantic-схемы.
  Поле нигде не читалось (вебхуки отправляются всегда), фронт и тесты его
  не использовали — проверено grep'ом по репо.
- `docs/tg/fyi.md`: «поле есть, но не работает, мы его уберём» → «вебхуки
  отправляются всегда, если пользователь подписан — отдельного флага в
  запросе нет». API больше не обещает того, чего не делает.

Поведение рантайма не изменилось: поле игнорировалось и раньше. Запросы со
старым `trigger_webhook` в теле пройдут (pydantic отбрасывает неизвестные
поля по умолчанию) — совместимость по железу сохранена.

Тесты: 735 backend-тестов и полный гейт — зелёные.
