# Docker Registry Configuration
# Leave empty for local builds, or set to your registry URL for pushing images
# Examples:
#   - Docker Hub: DOCKER_REGISTRY=username
#   - GitHub Container Registry: DOCKER_REGISTRY=ghcr.io/username
#   - Google Artifact Registry: DOCKER_REGISTRY=europe-west1-docker.pkg.dev/project-id/repository
#   - AWS ECR: DOCKER_REGISTRY=123456789.dkr.ecr.region.amazonaws.com
DOCKER_REGISTRY=

# Image Tag (default: latest)
# Use semantic versioning (e.g., v1.0.0) or custom tags (e.g., dev, staging)
IMAGE_TAG=latest

# Examples of usage:
# 1. Local build (no registry):
#    docker compose -f docker-compose-v2.yml build
#    => Images: presenton-caddy:latest, presenton-nextjs:latest, presenton-fastapi:latest
#
# 2. Build and push to Docker Hub:
#    export DOCKER_REGISTRY=myusername
#    export IMAGE_TAG=v1.0.0
#    docker compose -f docker-compose-v2.yml build
#    docker compose -f docker-compose-v2.yml push
#    => Images: myusername/presenton-caddy:v1.0.0, etc.
#
# 3. Build and push to Google Artifact Registry:
#    export DOCKER_REGISTRY=europe-west1-docker.pkg.dev/my-project/presenton
#    docker compose -f docker-compose-v2.yml build
#    docker compose -f docker-compose-v2.yml push

# Internal Service URLs
# These URLs are used for inter-service communication within Docker
# Default values work for standard Docker Compose setup
PRESENTON_URL=http://caddy:80
NEXTJS_URL=http://nextjs:3000
